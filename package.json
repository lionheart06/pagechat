{
  "name": "pagechat-extension",
  "version": "1.0.0",
  "description": "A browser extension for enhanced page interactions",
  "private": true,
  "scripts": {
    "build": "npm run copy-files",
    "copy-files": "echo 'Extension ready for loading'",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "web-ext": "^7.0.0"
  },
  "keywords": ["chrome-extension", "firefox-extension", "browser-extension"],
  "author": "Your Name",
  "license": "MIT"
}